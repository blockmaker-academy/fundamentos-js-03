// Reemplazar subcadena
let sentencia = "Reemplazaremos una subcadena por otra, con el método replace"
const nuevaSentencia = sentencia.replace("Reemplazaremos", "Cambiaremos") //El primer argumento es el que se quita y el segundo el que se añade

console.log(nuevaSentencia);
