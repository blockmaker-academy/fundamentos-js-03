// Redondeo arriba. Con la función Math.ceil(), se redondea un decimal hacia arriba.

const valor = 2.1
const redondear = Math.ceil(valor)

console.log("Su valor " + valor +" redondeado superior es " + redondear)
