// Copia superficial de un Array. El método .slice(), coge un porción del array, indicandole el iíndice

const numeros = [1, 2, 3, 4, 5, 6];
const porcion = numeros.slice(0,4);

console.log("A nuestra lista " + numeros + " le rebanamos " + porcion)



