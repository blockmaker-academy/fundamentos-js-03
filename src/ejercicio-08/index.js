// Verificar Inclusión
let texto = "Verificando si existe subcadena con el método includes";
let buscar = "subcadena";
let veredicto = texto.includes(buscar); //Este método arroja true o false. 
console.log(veredicto)