// Dividir subcadenas
let cadena = "Dividiremos la cadena en una array con el método split";
let array = cadena.split(" ") //Entre paréntesis va el separador que tendrá en cuenta el método split, por donde se harán las divisiones.
console.log(array)
