// Convertir a cadena
//Método .tostring, convierte un número en cadena de texto
let numero = 35
let string = numero.toString();

console.log( string + string);

//Atención al nombre del método tipo camelcase. Con la S minúscula no funciona