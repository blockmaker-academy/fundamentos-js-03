// Encontrar subcadena
let sentencia = "Encuentra posición";
let posicion = sentencia.indexOf("ó")

console.log(posicion);
// Si hay subcadenas repetidas, solo muestra la posición de la primera subcadena encontrada.

